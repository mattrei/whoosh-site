{% set bodyClass = entry.slug %}
{% set title = entry.title %}

{% extends '_layout' %}

{% block content %}
	
	<div class="xl:container px-3  mx-auto mt-24">
		<h1 class="text-6xl uppercase font-bold text-center mb-6">
			{{ entry.title }}
		</h1>
		
		<div class="contentBlocks">
			{% include '_contentBlocks' %}
		</div>
		
		{% if entry.slug == "about-us" %}
			<div class="team mt-16 grid grid-cols-4 gap-4 items-center">
				
				<h2 class="text-4xl uppercase font-bold text-center mb-4 col-span-2">Team</h2>
				
				{% for person in craft.entries.section('team').all %}
					<a href="{{ person.url }}">
						{% if person.portrait | length %}
							{% set img = person.portrait.one %}
							<img class="rounded-lg h-auto" src="{{ img.getUrl }}"
							     alt="Portrait von {{ person.title }}"
							     srcset="{% include '_srcset' %}"
							     width="{{ img.width }}"
							     height="{{ img.height }}"
							     sizes="23vw, (min-width: 1280px) 310px">
						
						{% else %}
							{{ person.title }}
						{% endif %}
					</a>
				{% endfor %}
			</div>
		
		{% elseif entry.slug == "manifesto" %}
				
		{% endif %}
	</div>

{% endblock %}
